% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_source.R
\name{.tm_run}
\alias{.tm_run}
\title{Run the tasks and reassemble one profile}
\usage{
.tm_run(tasks, src, window, slide, model, BPPARAM, keep_sequence, verbose)
}
\arguments{
\item{tasks}{From \code{\link{.tm_tasks}}.}

\item{src}{A source from \code{\link{.tm_source}}.}

\item{window, slide}{Tiling; \code{NULL} window means one window per region.}

\item{model}{Model arguments to hand to \code{\link{tm_calculate}}.}

\item{BPPARAM}{A \code{BiocParallelParam}, or \code{NULL} for this process.}

\item{keep_sequence}{Keep the sequence columns in the result.}

\item{verbose}{Report task and window counts.}
}
\value{
A \code{GRanges} in source order.
}
\description{
Each task opens the source itself, so what crosses between processes is a
name and a coordinate pair rather than any sequence. That is what makes
parallelism pay here, and why an in-memory source is staged to a file
first rather than sent over a socket.
}
\keyword{internal}
