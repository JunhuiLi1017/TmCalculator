% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_source.R
\name{.tm_as_granges}
\alias{.tm_as_granges}
\title{Read `regions` as an interval query against a GRanges source}
\usage{
.tm_as_granges(regions, src)
}
\arguments{
\item{regions}{Character or numeric regions.}

\item{src}{A GRanges source from \code{\link{.tm_source}}.}
}
\value{
A \code{GRanges} to overlap against, or \code{NULL} for positional.
}
\description{
\code{"chr1"} means the whole of that \code{seqname} and
\code{"chr1:1000-2000"} means that interval of it. A vector of bare
integers is positional instead, the nth ranges, which is what the same
input means for a FASTA file or for sequences; \code{NULL} is returned in
that case so the caller resolves it by position.
}
\keyword{internal}
