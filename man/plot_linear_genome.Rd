% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_linear_genome.R
\name{plot_linear_genome}
\alias{plot_linear_genome}
\title{Plot linear genome tracks using karyoploteR}
\usage{
plot_linear_genome(
  genome_name,
  genome_size = NULL,
  genome = NULL,
  track_list,
  label = NULL,
  chromosomes = NULL,
  plot.type = 1,
  track.gap = 0.01,
  legend.show = TRUE,
  legend.position = "topright",
  legend.cex = 0.6,
  legend.bty = "n",
  legend.border = NA,
  axis.cex = 0.5,
  title.cex = 0.9,
  base.tick.dist = NULL
)
}
\arguments{
\item{genome_name}{Character. Used for the title.}

\item{genome_size}{Numeric. Total length (single-chromosome genomes like E. coli).
Alternatively pass `genome` directly (GRanges) for multi-chromosome plots.}

\item{genome}{Optional GRanges describing the genome. If NULL, built from
genome_name + genome_size as a single contig.}

\item{track_list}{List of track specs (see above).}

\item{label}{data.frame/GRanges with seqnames/start/end/label (like circos version).}

\item{chromosomes}{Optional character vector to restrict/reorder chromosomes.}

\item{plot.type}{karyoploteR plot.type (1 = tracks above only, 2 = above+below).}

\item{track.gap}{Relative gap between tracks (0 to ~0.05).}

\item{legend.show}{Logical.}

\item{legend.position}{Passed to graphics::legend (e.g. "topright", or x/y).}

\item{legend.cex, legend.bty, legend.border}{See graphics::legend.}

\item{axis.cex}{Base numbers size.}

\item{title.cex}{Title size.}

\item{base.tick.dist}{Numeric or \code{NULL}. Distance between base-number
ticks passed to \code{karyoploteR::kpAddBaseNumbers}. When \code{NULL},
a default is chosen based on total genome size.}
}
\value{
Invisibly returns the KaryoPlot object.
}
\description{
Linear equivalent of plot_circos_genome(). Takes the same track_list
format so the same input can produce either view.
}
\details{
Supported track types in each list element:
  - type = "rect" : data as GRanges/data.frame, drawn as rectangles
  - type = "line" : data as GRanges/data.frame with value_col, drawn as lines
  - type = "area" : same as line but filled (ribbon from 0 to value)
  - type = "region": piled-up regions via kpPlotRegions (good for "ssDNA"-style tracks)
  - type = "coverage": coverage area plot from a set of regions

Each list element may also contain: name, col, bg.col, border, ylim, lwd
}
